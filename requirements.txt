fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
httpx==0.28.1
# GigaChain — обёртка Сбера над langchain. Нужна ради строгой схемы ответа:
# with_structured_output отдаёт JSON Schema прямо в API, и модель не может
# ответить произвольным текстом. Тянет за собой langchain-core.
langchain-gigachat==0.5.1
gigachat==0.2.3
python-dotenv==1.0.1

# аутентификация: JWT + хэширование паролей
PyJWT==2.10.1
bcrypt==4.2.1
email-validator==2.2.0

# только для тестов
pytest==8.3.4
pytest-asyncio==0.25.0
aiosqlite==0.20.0
